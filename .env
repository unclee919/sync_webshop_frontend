# Copy this file to .env and fill in the values for THIS deployment.
# The React app code never changes between servers - only these values do.

# The ERPNext server this storefront talks to (must be listed in that
# server's Webshop API Settings > Allowed Frontend Origins)
VITE_API_BASE_URL=http://erpnext.localhost:8000
